name: Build

on:
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ "*" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Install libgtkmm-4.0-0
      run: sudo apt-get install -y libgtkmm-4.0-0

    - name: Build
      run: make
